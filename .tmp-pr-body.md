## Plan / Issues

- **Plan**: N/A
- **Issues**: N/A

## Closes

N/A

## Type of Change

- [x] Project Plan update
- [ ] Execution Plan (new/updated plan)
- [ ] Feature implementation
- [ ] Bug fix
- [ ] Refactor
- [x] Documentation only
- [ ] Chore (CI, tooling, deps)

## Human Instructions / Intent

Human instruction: `ww projectでproject planを元に次に何が改善できるか一度棚卸してほしい。そして、それと今の利用者である私が改善を求めている部分とマッチングして優先度を立てて、優先するものをベースに次のPhaseを追加して、現在未対応のPhaseの順序も見直したい。あと、project-planのFRやNFRに実現済みなのか未実現なのかを明確にマークを付けたい。`
### Additional Context from Instructing Human

- The current user does not see agent skill packaging as a pressing need; `ww create`, `ww cd`, and a documented `--sandbox` usage example are enough for the common agent flow.
- The next priority should instead focus on globally managed config and hooks so many work repos do not each need a committed or locally maintained `.ww.toml`.
- The desired direction is a global config such as `$HOME/.ww/config.toml` with per-target-project matching anchored on the main worktree, plus hook support for sandbox-friendly setup such as symlinking `.env`, dependency directories, or caches from the main worktree.
- The user wants the roadmap to acknowledge a safer operating model for sandboxed agents: allow reads from `$HOME/.ww/*` but recommend denying writes to reduce prompt-injection or config-tampering risk.
- Claude Code issue `anthropics/claude-code#13195` is now closed, so `FR-25` should not keep assuming that specific issue is still open; however, the roadmap should still reflect that full sandbox compatibility needs verification beyond that one issue closure.
- A follow-up correction was requested: treat this PR as docs-only work and remove the unnecessary exec-plan file instead of pretending there is still pending plan work.

## Verification

- [ ] Tests pass (command: `N/A - docs only`)
- [ ] Lint passes (command: `N/A - docs only`)
- [x] Manual verification (describe below)

Manual verification:

- `git diff --check`
- reviewed the updated `docs/project-plan.md` wording against shipped specs/code for FR/NFR status alignment

## Checklist

- [x] Branch created from latest `origin/main`
- [x] `docs/specs/` updated (Spec-Code Parity) — _if code changed_
- [ ] Plan moved from `todo/` to `done/` — _if executing a plan_
- [x] Resolved linked local issues from the plan's `Addresses:` line were moved to `docs/issues/done/`, or this PR explains why they remain open
- [x] External GitHub issues declared in the plan's `Addresses:` line are linked under `Issues`, and implementation PRs include matching `Closes` entries or explain why they remain open
- [x] Workflow-linter warnings reviewed; all `fixable` warnings were resolved or explicitly justified in this PR
- [x] New issues logged in `docs/issues/` — _if discovered during work_
- [x] No unresolved blockers remain

## Dependencies

N/A

## Reviewer Notes

- This PR is intentionally docs-only. Review the roadmap reprioritization and FR/NFR status labels, not execution-plan workflow changes.
- Focus on whether the new priority order matches current real-world usage: global config and stronger hook workflows before trust hardening or agent-skill packaging.
- Check the FR/NFR status labels for over- or under-claiming, especially `FR-1`, `FR-12`, `FR-17`, `FR-18`, and `FR-25`.

## Links

- https://github.com/anthropics/claude-code/issues/13195
- https://github.com/anthropics/claude-code/issues/21942

## Breaking Changes

N/A
