name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c # v6.4.0
        with:
          go-version: "1.25"
      - run: make lint
      - run: make test-all
      - run: make build

  performance:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c # v6.4.0
        with:
          go-version: "1.25"
      - name: Evaluate advisory performance budget
        run: |
          started=$(date +%s)
          set +e
          output=$(make perf-check 2>&1)
          status=$?
          elapsed=$(( $(date +%s) - started ))
          printf '%s\n' "$output"
          {
            echo "## Performance budget"
            echo
            echo "Duration: ${elapsed}s"
            echo
            echo "### Benchmark results"
            echo '```text'
            printf '%s\n' "$output"
            echo '```'
            if [ "$status" -eq 0 ]; then
              echo "Result: within the configured budgets"
            elif [[ "$output" == *"budget exceeded:"* ]]; then
              echo "Result: budget exceeded (advisory; investigate before merge)"
            else
              echo "Result: evaluator failed (advisory; investigate before merge)"
            fi
          } >> "$GITHUB_STEP_SUMMARY"
          exit "$status"
